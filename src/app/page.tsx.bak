'use client'

import { useLanguage } from '@/lib/LanguageContext'
import Navbar from '@/components/Navbar'
import CategoryGrid from '@/components/CategoryGrid'
import { Search, ArrowRight, Shield, Clock, Star } from 'lucide-react'
import Link from 'next/link'
import { useState } from 'react'
import { useRouter } from 'next/navigation'

export default function HomePage() {
    const { t } = useLanguage()
    const [searchQuery, setSearchQuery] = useState('')
    const router = useRouter()

    const handleSearch = (e: React.FormEvent) => {
        e.preventDefault()
        if (searchQuery.trim()) {
            router.push(`/search?q=${encodeURIComponent(searchQuery)}`)
        }
    }

    return (
        <div className="min-h-screen bg-[var(--coffee-cream)] relative overflow-hidden">
            {/* Background blobs */}
            <div className="absolute top-0 left-0 w-full h-full overflow-hidden z-0 pointer-events-none">
                <div className="absolute top-[-10%] right-[-5%] w-[500px] h-[500px] rounded-full bg-[var(--water-fresh)] opacity-20 filter blur-[100px] animate-[float_6s_ease-in-out_infinite]" />
                <div className="absolute bottom-[-10%] left-[-10%] w-[600px] h-[600px] rounded-full bg-[var(--coffee-latte)] opacity-20 filter blur-[100px] animate-[float_8s_ease-in-out_infinite_reverse]" />
            </div>

            <Navbar />

            {/* Hero Section */}
            <section className="relative z-10 pt-20 pb-32 px-4 sm:px-6 lg:px-8 text-center">
                <div className="max-w-5xl mx-auto">
                    {/* Badge */}
                    <div className="inline-flex items-center gap-2 px-5 py-2 bg-white/60 backdrop-blur-sm rounded-full text-[var(--coffee-espresso)] text-sm font-medium mb-8 shadow-sm animate-in fade-in slide-in-from-bottom-4 duration-700">
                        <Star size={16} className="fill-[var(--coffee-latte)] text-[var(--coffee-latte)]" />
                        <span>O'zbekistonning #1 xizmatlar platformasi</span>
                    </div>

                    {/* Title */}
                    <h1 className="text-5xl sm:text-6xl lg:text-7xl font-display font-bold text-[var(--coffee-espresso)] mb-8 leading-tight animate-in fade-in slide-in-from-bottom-4 duration-700 delay-100">
                        {t('home.title')}
                    </h1>

                    {/* Subtitle */}
                    <p className="text-xl text-[var(--coffee-espresso)]/80 mb-12 max-w-2xl mx-auto font-sans leading-relaxed animate-in fade-in slide-in-from-bottom-4 duration-700 delay-200">
                        {t('home.subtitle')}
                    </p>

                    {/* Search Bar */}
                    <form onSubmit={handleSearch} className="relative max-w-2xl mx-auto mb-20 animate-in fade-in slide-in-from-bottom-4 duration-700 delay-300">
                        <div className="relative flex items-center p-2 bg-white rounded-full shadow-xl shadow-[var(--coffee-espresso)]/5 ring-1 ring-[var(--coffee-espresso)]/5">
                            <Search className="absolute left-6 text-[var(--coffee-latte)]" size={24} />
                            <input
                                type="text"
                                value={searchQuery}
                                onChange={(e) => setSearchQuery(e.target.value)}
                                placeholder={t('home.searchPlaceholder')}
                                className="w-full py-4 pl-14 pr-40 text-lg bg-transparent border-none focus:ring-0 placeholder:text-gray-400 text-[var(--coffee-espresso)] font-sans"
                            />
                            <button
                                type="submit"
                                className="absolute right-2 px-8 py-3 bg-[var(--coffee-espresso)] text-[var(--coffee-cream)] rounded-full font-bold hover:bg-[var(--water-deep)] transition-all duration-300 flex items-center gap-2 shadow-lg hover:shadow-xl hover:-translate-y-0.5"
                            >
                                {t('nav.search')}
                                <ArrowRight size={18} />
                            </button>
                        </div>
                    </form>

                    {/* Quick stats */}
                    <div className="flex flex-wrap justify-center gap-12 animate-in fade-in slide-in-from-bottom-4 duration-700 delay-400">
                        <div className="flex flex-col items-center gap-2 group">
                            <div className="w-16 h-16 rounded-2xl bg-white shadow-lg flex items-center justify-center text-[var(--water-deep)] mb-2 group-hover:-translate-y-2 transition-transform duration-300">
                                <Shield size={32} />
                            </div>
                            <span className="font-sans font-medium text-[var(--coffee-espresso)]">Ishonchli ustalar</span>
                        </div>
                        <div className="flex flex-col items-center gap-2 group">
                            <div className="w-16 h-16 rounded-2xl bg-white shadow-lg flex items-center justify-center text-[var(--coffee-espresso)] mb-2 group-hover:-translate-y-2 transition-transform duration-300">
                                <Clock size={32} />
                            </div>
                            <span className="font-sans font-medium text-[var(--coffee-espresso)]">24/7 qo'llab-quvvatlash</span>
                        </div>
                        <div className="flex flex-col items-center gap-2 group">
                            <div className="w-16 h-16 rounded-2xl bg-white shadow-lg flex items-center justify-center text-[var(--coffee-latte)] mb-2 group-hover:-translate-y-2 transition-transform duration-300">
                                <Star size={32} className="fill-current" />
                            </div>
                            <span className="font-sans font-medium text-[var(--coffee-espresso)]">1000+ baholangan usta</span>
                        </div>
                    </div>
                </div>
            </section>

            {/* Categories Section */}
            <section className="py-24 relative z-10">
                <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                    <div className="flex items-center justify-between mb-12">
                        <h2 className="text-3xl sm:text-4xl font-display font-bold text-[var(--coffee-espresso)]">
                            {t('home.allCategories')}
                        </h2>
                        <Link
                            href="/search"
                            className="text-[var(--water-deep)] hover:text-[var(--coffee-espresso)] font-bold flex items-center gap-2 transition-colors group"
                        >
                            {t('home.viewAll')}
                            <ArrowRight size={20} className="group-hover:translate-x-1 transition-transform" />
                        </Link>
                    </div>
                    <CategoryGrid />
                </div>
            </section>

            {/* How it works */}
            <section className="py-24 bg-white/50 backdrop-blur-sm relative z-10">
                <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                    <h2 className="text-3xl sm:text-4xl font-display font-bold text-[var(--coffee-espresso)] text-center mb-20">
                        Qanday <span className="text-[var(--water-deep)] italic">ishlaydi?</span>
                    </h2>

                    <div className="grid md:grid-cols-3 gap-12 relative">
                        {/* Connecting line */}
                        <div className="hidden md:block absolute top-12 left-[16%] right-[16%] h-0.5 bg-gradient-to-r from-[var(--water-fresh)] via-[var(--coffee-latte)] to-[var(--water-fresh)] border-t border-dashed border-gray-300 z-0" />

                        {/* Step 1 */}
                        <div className="relative z-10 text-center group">
                            <div className="w-24 h-24 mx-auto mb-8 rounded-full bg-white border-4 border-[var(--water-fresh)] flex items-center justify-center text-[var(--water-deep)] text-4xl font-display font-bold shadow-xl group-hover:scale-110 transition-transform duration-300">
                                1
                            </div>
                            <h3 className="text-2xl font-display font-bold text-[var(--coffee-espresso)] mb-4">Xizmat tanlang</h3>
                            <p className="text-gray-600 leading-relaxed px-4">
                                Sizga kerakli xizmatni kategoriya yoki ism bo'yicha qidiring
                            </p>
                        </div>

                        {/* Step 2 */}
                        <div className="relative z-10 text-center group">
                            <div className="w-24 h-24 mx-auto mb-8 rounded-full bg-white border-4 border-[var(--coffee-latte)] flex items-center justify-center text-[var(--coffee-espresso)] text-4xl font-display font-bold shadow-xl group-hover:scale-110 transition-transform duration-300">
                                2
                            </div>
                            <h3 className="text-2xl font-display font-bold text-[var(--coffee-espresso)] mb-4">Vaqt belgilang</h3>
                            <p className="text-gray-600 leading-relaxed px-4">
                                Kalendardan qulay kun va soatni tanlang, narxni ko'ring
                            </p>
                        </div>

                        {/* Step 3 */}
                        <div className="relative z-10 text-center group">
                            <div className="w-24 h-24 mx-auto mb-8 rounded-full bg-white border-4 border-[var(--water-deep)] flex items-center justify-center text-[var(--water-deep)] text-4xl font-display font-bold shadow-xl group-hover:scale-110 transition-transform duration-300">
                                3
                            </div>
                            <h3 className="text-2xl font-display font-bold text-[var(--coffee-espresso)] mb-4">Chat orqali gaplashing</h3>
                            <p className="text-gray-600 leading-relaxed px-4">
                                Usta bilan bevosita bog'laning va lokatsiyangizni jo'nating
                            </p>
                        </div>
                    </div>
                </div>
            </section>

            {/* CTA Section */}
            <section className="py-24 relative z-10">
                <div className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
                    <div className="relative overflow-hidden rounded-[3rem] bg-[var(--water-deep)] p-12 sm:p-20 text-center shadow-2xl">
                        {/* Background Pattern */}
                        <div className="absolute inset-0 opacity-10 bg-[radial-gradient(#fff_1px,transparent_1px)] [background-size:16px_16px]" />

                        <div className="relative z-10">
                            <h2 className="text-3xl sm:text-5xl font-display font-bold text-white mb-6 leading-tight">
                                Siz ham usta bo'lmoqchimisiz?
                            </h2>
                            <p className="text-[var(--water-fresh)]/90 mb-10 max-w-xl mx-auto text-lg">
                                O'z xizmatlaringizni platformaga qo'shing va minglab mijozlarga yetib boring
                            </p>
                            <Link
                                href="/auth/register"
                                className="inline-flex items-center gap-3 px-10 py-5 bg-[var(--coffee-latte)] text-[var(--coffee-espresso)] text-lg font-bold rounded-full hover:bg-white transition-all duration-300 shadow-xl hover:-translate-y-1"
                            >
                                Ro'yxatdan o'tish
                                <ArrowRight size={24} />
                            </Link>
                        </div>
                    </div>
                </div>
            </section>

            {/* Footer */}
            <footer className="bg-[var(--coffee-espresso)] text-[var(--coffee-cream)] py-16">
                <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                    <div className="flex flex-col md:flex-row justify-between items-center gap-8">
                        <div className="flex items-center gap-3">
                            <div className="w-12 h-12 bg-[var(--coffee-cream)] rounded-full flex items-center justify-center">
                                <span className="text-[var(--coffee-espresso)] font-display font-bold text-2xl">B</span>
                            </div>
                            <span className="text-2xl font-display font-bold text-white tracking-tight">Bek.uz</span>
                        </div>
                        <p className="text-sm text-[var(--coffee-latte)] opacity-60">
                            © 2026 Bek.uz. All rights reserved.
                        </p>
                    </div>
                </div>
            </footer>
        </div>
    )
}
